context("lme objects")
# library(nlme)
# data("Orthodont", package = "nlme")
# obj <- lme(distance ~ age, random = ~ age, data = Orthodont)
# vcov <- vcovCR(obj, type = "CR2")

# test cluster specification
# test target matrix specification
# test inverse_var detection